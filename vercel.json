{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "npm cache clean --force && npm install",
        "buildCommand": "npm run build"
      }
    }
  ]
}
